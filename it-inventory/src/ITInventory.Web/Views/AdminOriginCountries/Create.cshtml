@model ITInventory.Web.Models.Admin.OriginCountryFormViewModel
@{
    ViewData["Title"] = "New Origin Country";
}

<h2 class="text-xl font-semibold text-slate-900 mb-6">New Origin Country</h2>

<div class="bg-white rounded-lg shadow-sm border border-slate-200 p-6 max-w-2xl">
    <form asp-action="Create" method="post">
        @await Html.PartialAsync("_Form", Model)
    </form>
</div>
