@model ITInventory.Web.Models.Admin.CompanyFormViewModel
@{
    ViewData["Title"] = "New Company";
}

<h2 class="text-xl font-semibold text-slate-900 mb-6">New Company</h2>

<div class="bg-white rounded-lg shadow-sm border border-slate-200 p-6 max-w-3xl">
    <form asp-action="Create" method="post">
        @await Html.PartialAsync("_Form", Model)
    </form>
</div>
